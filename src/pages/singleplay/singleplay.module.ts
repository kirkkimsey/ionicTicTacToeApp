import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SingleplayPage } from './singleplay';

@NgModule({
  declarations: [
    SingleplayPage,
  ],
  imports: [
    IonicPageModule.forChild(SingleplayPage),
  ],
})
export class SingleplayPageModule {}
